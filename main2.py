import read_file

days = 10
register = true

numDays, librerias, score = read_file.read_file('')

# Por cada dia
for day in days:

    # Hay alguien registrandose?
    if not register:

        # Meter a una libreria a registrar
        # Coger libreria mas beneficiosa
        # Poner a registrar
        register = true
    
    library_register.register_time--



    # Mandar libros de todas las librerias
    for library in library_register
        
        # Enviar n libros de libreria mas valiosos
        # Quitar esos libros de la libreria
        # Quitar esos libros de todas las librerias

    if libraryRegister.register_days == 0:
        libraries_scanning.append(library_register)
        register = false

